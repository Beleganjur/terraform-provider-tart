package main

import "testing"

// TestBuild ensures the package builds and test harness can run.
func TestBuild(t *testing.T) {
	// No-op: this test exists to make sure the executor package participates in `go test ./...`.
}
